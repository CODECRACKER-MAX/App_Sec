name: CI

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        
      # Install Semgrep
      - name: Install Semgrep
        run: pip install semgrep

      # Run Semgrep scan
      - name: Run Semgrep
        run: semgrep --config=auto . --sarif --output=semgrep.sarif

      # Save the semgrep report
      - name: Upload SARIF file to GitHub Security Tab
        uses: github/codeql-action/upload-sarif@3
        with:
          name: semgrep.sarif
          
      # Install trivy
      - name: Trivy Installation & Scanning
        run: |
          sudo apt-get install wget apt-transport-https gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install trivy
          
          # Scan trivy report
          - name: Run Trivy Scan via CLI      
          trivy fs . --scanners vuln,misconfig,secret,license --format sarif --output trivy.sarif
                 
      - name: Trivy Scan Results Upload to GitHub
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-everything-report.sarif'

      # Run ZAP using GitHub ACTIONS
      - name: Run OWASP ZAP Full Scan
        # Uses the official, stable ZAP Full Scan marketplace action
        uses: zaproxy/action-full-scan@v0.12.0
        with:
          target: 'http://127.0.0.1:3000'
          
          # Disables filing a duplicate GitHub issue for discovered alerts
          allow_issue_writing: false
          
          # Force the container script to generate a standard SARIF JSON report file
          cmd_options: '-w zap-report.sarif'

      - name: Upload ZAP Alerts to GitHub Security Tab
        uses: github/codeql-action/upload-sarif@v3
        # Ensures that even if ZAP finds critical flaws, the report is still uploaded
        if: always() 
        with:
          sarif_file: 'zap-report.sarif'
          
      - name: Nuclei Scanning....
        uses: projectdiscovery/nuclei-action@v3
        with:
          # Define the target application or staging environment URL
          args: "-u http://127.0.0.1:3000 -format sarif -output nuclei-report.sarif"

      - name: Upload Nuclei Alerts to GitHub Security Tab
        uses: github/codeql-action/upload-sarif@v3
        if: always() # Guarantees report uploading even if active flaws are discovered
        with:
          sarif_file: 'nuclei-report.sarif'
